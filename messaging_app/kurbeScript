#!/bin/bash
# Script: kurbeScript
# Purpose: Start a local Kubernetes cluster using Minikube and verify setup

set -e

echo "Starting Minikube cluster..."
minikube start

echo "Cluster info:"
kubectl cluster-info

echo "Retrieving all pods (all namespaces):"
kubectl get pods -A

echo "Kubernetes cluster setup and verification complete!"
